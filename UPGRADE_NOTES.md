# Clwsd 升级笔记

## 1.0.0 功能

1. 从文件读取 URL
2. 逐个发起 HTTP/HTTPS 请求
3. 提取状态码、标题、响应长度、Server
4. 处理超时、连接失败、请求异常
5. 保存结果到输出文件

## 1.1.0 升级点

1. 读取目标时自动去重
2. 增加 `final_url`
3. 增加 `content_type`
4. 增加 `-a / --only-alive`，只输出存活目标
5. 输出字段更完整，便于后续分析

## 1.2.0 升级点

1. 增加 `-w / --workers` 参数
2. 使用 `ThreadPoolExecutor` 实现多线程并发探测
3. 使用 `as_completed()` 按完成顺序处理结果
4. 提高多目标探测速度
5. 增加线程任务异常捕获，避免单个任务影响整体运行

## 1.3.0 升级点

1. 增加 `-f / --format` 参数
2. 支持 TXT 输出
3. 支持 CSV 输出
4. 支持 JSON 输出
5. 为后续结构化分析做准备

## 2.0.0 升级点

1. 完成项目模块化拆分
2. 将 URL 读取逻辑迁移到 `core/loader.py`
3. 将探测和标题提取逻辑迁移到 `core/checker.py`
4. 将 TXT / CSV / JSON 输出逻辑迁移到 `core/output.py`
5. 将 `clwsd.py` 精简为 CLI 入口和线程调度层

## 2.1.0 升级点

1. 增加参数合法性校验
2. 限制 `-t / --timeout` 必须大于等于 1
3. 限制 `-w / --workers` 必须大于等于 1
4. 对非法输出格式增加显式报错
5. 提高工具对错误输入的健壮性

## 2.2.0 升级点

1. 增加 URL 基础预清洗
2. 去掉每行首尾空白
3. 去掉 UTF-8 BOM
4. 对缺少协议头的目标自动补全 `http://`
5. 在清洗后的结果基础上进行去重

## 2.3.0 升级点

1. 增加终端彩色输出
2. `2xx` 状态码整行显示为绿色
3. `3xx` 状态码整行显示为黄色
4. `4xx` / `5xx` / `ERROR` 整行显示为红色
5. 不影响 TXT / CSV / JSON 文件输出

## 2.4.0 升级点

1. 新增 `core/display.py`
2. 将 Banner 打印逻辑迁移到显示层模块
3. 将单条结果打印逻辑迁移到显示层模块
4. 将终端摘要输出逻辑迁移到显示层模块
5. 进一步精简 `clwsd.py`，让主入口只负责调度

## 2.5.0 升级点

1. 增加超时自动重试机制
2. 当目标第一次请求超时时，默认自动再请求 1 次
3. 第二次仍然超时才返回 `timeout`
4. 不新增命令行参数，保持默认行为简单
5. 提高存活探测结果的稳定性，减少临时超时误判

## 2.6.0 升级点

1. 默认只保留高价值存活结果
2. 将 `200`、`301`、`302`、`401`、`403` 视为存活结果
3. 为每个目标增加优先级分数、等级和原因
4. 根据标题和 URL 关键词提升优先级
5. 按优先级排序显示和保存结果，并用不同颜色进行标记

## 2.7.0 升级点

1. 默认输出目录改为 `result/`
2. 第一次使用时自动创建 `result/` 目录
3. 默认生成 `html + csv` 双格式结果
4. 支持通过 `-f` 选择 `txt` / `json` 输出
5. 输出字段精简为 `网站`、`响应码`、`标题`、`优先级`
6. HTML 报告中的网站链接可直接点击访问
7. HTML 报告改为白色背景并保留优先级行着色

## 2.8.0 升级点

1. 优化 HTML 报告整体排版
2. 增加顶部统计摘要卡片
3. 展示扫描总数、当前展示数和高/中/低优先级数量
4. 当 `--all` 模式下存在非存活目标时，额外显示非存活数量
5. 增加生成时间信息，方便归档和复盘

## 2.9.0 升级点

1. 为 HTML 报告增加交互式筛选工具栏
2. 支持一键切换查看全部、高优先级、中优先级、低优先级结果
3. 当存在非存活目标时，支持单独筛选非存活结果
4. 顶部统计卡片可点击联动筛选
5. 实时显示当前筛选状态和可见结果数量

## 3.0.0 升级点

1. 为 HTML 报告增加关键词搜索框
2. 支持搜索网址、标题、状态码
3. 增加排序下拉框
4. 支持按默认顺序、优先级、状态码、网址、标题进行排序
5. 搜索、筛选、排序三者可联动工作，提升人工研判效率

## 3.1.0 升级点

1. 在 `core/checker.py` 中引入线程内 `Session` 连接复用
2. 使用 `HTTPAdapter` 建立连接池，减少重复建连开销
3. 请求改为流式读取，避免默认下载整页内容
4. 仅预读页面前部内容提取标题，降低大页面目标的带宽和时间消耗
5. 将超时重试逻辑改为循环结构，后续更容易继续扩展

## 3.2.0 升级点

1. 将请求超时拆分为连接超时和读取超时
2. 对连接阶段使用更短的超时上限，减少无效等待
3. 为读取阶段保留独立超时，避免可访问但响应较慢的目标被过早放弃
4. 为 `Session` 增加最大跳转次数限制
5. 对异常跳转链返回 `too_many_redirects`，避免个别目标拖慢整体扫描

## 3.3.0 升级点

1. 对明显非 HTML 的响应更早停止正文预读
2. 优化预读缓冲逻辑，减少无意义的重复字节拼接
3. 为错误结果增加 `error_type` 分类字段
4. 区分 `timeout`、`connection_error`、`too_many_redirects`、`ssl_error` 等错误类型
5. 在 `--all` 模式下增加终端错误分布摘要，方便定位性能瓶颈和异常类型

## 3.4.0 升级点

1. 为部分静态资源和下载类 URL 增加 `HEAD -> GET` 自适应探测策略
2. 对明显不需要正文内容的目标优先使用 `HEAD`，减少不必要的页面下载
3. 当 `HEAD` 返回的内容类型仍可能需要标题或页面特征时，自动回退到 `GET`
4. 对不支持 `HEAD` 的目标自动回退到 `GET`
5. 在保持标题提取与优先级分析能力的同时，进一步降低部分目标的探测成本

## 3.5.0 升级点

1. 将扫描调度从“全部一次性提交”改为“滑动窗口提交”
2. 默认线程数从 `10` 提升到 `30`
3. 扫描过程中只维持有限数量的在途任务，避免大规模 URL 时一次性堆积过多 future
4. 每当有任务完成时，立即补充新的目标进入线程池，保持并发持续稳定
5. 提高 `1000+` 目标场景下的资源利用效率和整体稳定性

## 3.6.0 升级点

1. 新增 `core/fingerprinter.py`，支持轻量指纹识别
2. 从 `Server`、`Title`、`URL`、关键响应头中提取标签
3. 支持识别 `nginx`、`iis`、`apache`、`tomcat`、`login-page`、`admin-page`、`oa`、`basic-auth` 等标签
4. 将指纹标签接入优先级分析，使更有价值的目标更容易靠前
5. 在终端结果和 HTML 报告中输出指纹标签，提升实战筛选效率

## 3.7.0 升级点

1. 将输出字段从 4 列升级为 5 列，新增 `指纹标签`
2. 指纹标签同步写入 `HTML`、`CSV`、`TXT`、`JSON`
3. HTML 报告改为五列表格：`网站`、`响应码`、`标题`、`指纹标签`、`优先级`
4. 指纹标签从标题下方移到独立列，人工查看更直观
5. HTML 搜索范围增加指纹标签，便于按组件和系统特征快速筛选

## 3.8.0 升级点

1. 细分 `admin-console`、`generic-default-page`、`directory-listing` 等指纹标签
2. 对默认欢迎页和默认管理壳做更强的降权，让低价值目标更稳定落到 `LOW`
3. 对后台控制台类页面增加更明确的加权，提升实战研判优先级
4. 对 `IIS7`、`Welcome to nginx!` 等常见默认页做更精准的识别
5. 进一步让 `high / medium / low` 的分层更符合人工打点习惯

## 3.9.0 升级点

1. 增加页面正文轻量采样，标题后继续读取一小段正文用于特征识别
2. 新增对 `password-input`、`upload-form`、`swagger-ui`、`jenkins`、`grafana` 等正文特征的识别
3. 让登录框、上传点、控制台类页面的优先级判断更准确
4. 对通用认证网关的密码框进行专门降权，避免认证入口被误抬高
5. 保持输出字段不变，只增强核心研判能力

## 4.0.0 升级点

1. 支持自定义 `Header`、`Cookie` 和代理请求
2. 支持从 `favicon` 和静态资源路径提取更多指纹标签
3. 增加 `WAF`、`软 404`、`跳转壳` 的降噪识别
4. 让假存活页更稳定落入低优先级
5. 继续保持输出结构不变，只提升实战研判质量

## 4.1.0 升级点

1. 指纹结果增加结构化 `technology_details`
2. 新增 `core/thirdparty_fingerprints.py`、`core/thirdparty_providers.py`、`core/technology_merge.py`
3. 增加第三方技术指纹适配层骨架，为后续接入外部库做准备
4. 接入可选 `python-Wappalyzer` provider，未安装依赖时自动跳过
5. 新增 `--thirdparty-fingerprints` 开关，默认关闭，保持普通扫描轻量
6. 增加主技术排序与次技术分组，输出更适合实战研判的技术结构
7. 明确 `clwsd` 的核心边界，后续深度识别建议改为外挂模块而不是继续堆入主程序
